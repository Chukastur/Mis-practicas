// Importación de módulos

import {add, pi, Circle} from "./28-export-modules.js"

// Funciones

console.log (add (5, 10))

// Propiedades

console.log (pi)

// Clases

let circle = new Circle (10)
console.log (circle.radius)
console.log (circle.area().toFixed(2))
console.log (circle.perimeter().toFixed(2))

//Importación por defecto

import resta from "./28-export-modules.js" // no hace falta coincidencia en nombre.Fue declarada default al exportar

console.log (resta (10, 5))

// Proyecto modular

//import { MyClass} from "./otracarpeta/Como-se-llame.js"